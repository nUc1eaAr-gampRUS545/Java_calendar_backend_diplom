spring:
  cache:
    type: redis
  autoconfigure:
    exclude: org.springframework.boot.autoconfigure.orm.jpa.HibernateJpaAutoConfiguration
      - org.springframework.boot.autoconfigure.data.redis.RedisAutoConfiguration
      - org.springframework.boot.autoconfigure.data.redis.RedisRepositoriesAutoConfiguration
  application:
    name: java_calendar
  datasource:
    url: jdbc:postgresql://localhost:6666/Java_calendar
    username: postgres
    password: qwerty
    driverClassName: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        packagesToScan: ru.minusd.security
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
  servlet:
    multipart:
      max-request-size: 10MB
      max-file-size: 10MB
  data:
    redis:
      host: localhost
      port: 6379
      password:

datasource:
  driver: org.postgresql.Driver
  url: jdbc:postgresql://localhost:6666/Java_calendar
  username: postgres
  password: qwerty
token:
  signing:
    key: 53A73E5F1C4E0A2D3B5F2D784E6A1B423D6F247D1F6E5C3A596D635A75327855

yandex:
  token: y0__xC84t-ZBRir7TUg06vovhJaZ4QoKQxrifigtmuonS3ZBJvCyw

hibernate:
  dialect: org.hibernate.dialect.PostgreSQLDialect
  show_sql: true
  batch:
    size: 50
  hbm2ddl:
    auto: update
  current:
    session:
      context:
        class: thread

management:
  endpoint:
    health:
      enabled: true
  endpoints:
    web:
      exposure:
        include: health